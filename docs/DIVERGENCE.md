# Divergence from upstream niri

Invariant I5: prefer new modules; when an inherited file must be edited, add a line here. Format: `path/to/file.rs — what changed, and why.`

## Edits to inherited files

(workspace-wide) — mechanical niri→swayward rename, with no behavioural change. See the rename commit.
swayward-ipc/src/lib.rs, swayward-ipc/src/legacy.rs, swayward-ipc/src/command.rs — retain inherited config vocabulary in `legacy` beside the sway schema until milestone 4 moves binds to sway command strings, carry internal window-move events for sway IPC translation, and parse sway's floating/tiling focus modes, directional move distances, and move-position forms.
src/ipc/client.rs, src/ipc/server.rs — import colliding inherited IPC types from `legacy` while the new sway schema keeps the bare names.
src/ipc/server.rs, src/ipc/wire.rs — replace niri's line-delimited request dispatch with tested sway binary framing, honest unsupported-message replies, sway's nonblocking power-of-two event write buffer ceiling, and sway's safe `SWAYSOCK` adoption and stale-socket lifecycle.
swayward-config/src/lib.rs, src/ipc/server.rs, src/swayward.rs — retain the raw top-level KDL source and return it through sway's single-field GET_CONFIG schema; included files remain excluded.
src/ipc/server.rs — omit unavailable keyboard-layout fields and events when the seat has no keyboard or its XKB state cannot be read, rather than panicking during the regular IPC refresh path.
src/ipc/server.rs — decode the request frame before parsing its type, so a request code outside `MessageType` gets sway's reply instead of a disconnect: `{"success": false}` for IPC_SYNC (`sway/sway/ipc-server.c:919-924`) and a structured not-implemented body otherwise (`ipc-server.c:927-929`).
src/ipc/server.rs — keep serving queries and commands on a subscribed connection, because sway's `IPC_SUBSCRIBE` only sets `subscribed_events` and leaves the client in the ordinary command path (`sway/sway/ipc-server.c:730-784`), and refresh the cached query state from the compositor before each stateful reply so a long-lived connection does not answer from its connect-time snapshot (sway serialises at request time, `ipc-server.c:815-823`).
src/ipc/server.rs, swayward-ipc/src/{legacy.rs,state.rs} — carry workspace tree nodes through urgency and output-move state changes so subscribers receive sway's `workspace::urgent` and `workspace::move` payloads instead of synthetic reloads, including when the workspace snapshot changes in the same refresh.
src/ipc/client.rs — remove niri's client because swaymsg is swayward's supported IPC client.
Cargo.toml, Cargo.lock, src/handlers/mod.rs, src/input/backend_ext.rs — update Smithay to its virtual-keyboard input-event API and route `zwp_virtual_keyboard_v1` through the normal compositor input path so bindings, modes, release handling, and idle activity see synthetic keys.
src/backend/winit.rs — disable Mesa swap throttling in nested sessions because a blocking EGL swap deadlocks the compositor event loop and IPC.
src/main.rs — select the existing headless backend and create 1280×720 outputs when `WLR_HEADLESS_OUTPUTS` is set, matching wlroots and enabling live multi-output trials without extending sway IPC.
src/protocols/virtual_pointer.rs — default an omitted optional virtual-pointer axis source to wheel without warning, retain a source sent before its axis within the pending frame like wlroots, and ignore zero absolute extents instead of generating non-finite coordinates.
src/protocols/screencopy.rs, src/handlers/mod.rs — reject capture requests for removed outputs before advertising a frame buffer, matching sway/wlroots' disabled-output failure path and preventing stale output resources from reaching rendering.
src/protocols/gamma_control.rs — make client gamma-ramp fds nonblocking and read from offset zero, matching wlroots and preventing a valid pipe fd from blocking the compositor event loop or an inherited file offset from rejecting exact data.
src/swayward.rs, src/handlers/mod.rs — expose gamma control to the headless test backend with a fixed fake ramp size and no-op hardware application, so real protocol tests cover fd validation, exclusivity, destruction, and output removal without pretending to verify DRM persistence.
src/protocols/output_management.rs — accept all-disabled output configurations like sway, key synthesized runtime output config by connector, cancel only still-ongoing configurations after an atomic state update, and test retained head and mode handles after their targets disappear.
src/utils/xwayland/satellite.rs — log a missing optional xwayland-satellite executable at info while retaining warnings for spawn, wait, and capability failures.
src/layout/mod.rs, src/layout/workspace.rs, src/layout/tile.rs — expose focused-tree layout mutation and direct container focus for sway runtime commands, including sway's parent targeting, workspace-level layer history, one-level singleton flatten, focus rank and parent preservation across floating transitions, fullscreen state for floating-origin command checks, and decoration-only activation outside a client's Wayland input region.
src/layout/workspace.rs, src/layout/tiling_tree/{mod.rs,geometry.rs} — derive `smart_no_gaps` border suppression from the already-resolved workspace area so positive and negative outer gaps match sway's `current_gaps` behavior.
src/layout/mod.rs, src/layout/workspace.rs, src/layout/tile.rs, src/layout/floating.rs, swayward-config/src/layout.rs, contrib/sway-to-kdl — expose focused-tree layout mutation and direct container focus for sway runtime commands, including sway's parent targeting, workspace-level layer history, one-level singleton flatten, focus rank and parent preservation across floating transitions, sway's configurable global floating size constraints and client-chosen initial size, clamped/centered default size for tiled windows entering the scratchpad, explicit failure when an edge resize is fully clamped, fullscreen clearing, and fullscreen state for floating-origin command checks.
src/layout/mod.rs, src/layout/workspace.rs, src/layout/tile.rs, src/layout/floating.rs, src/window/mapped.rs — expose focused-tree layout mutation and direct container focus for sway runtime commands, including sway's parent targeting, workspace-level layer history, one-level singleton flatten, focus rank and parent preservation across floating transitions, proportional center remapping from the saved source area when scratchpad windows cross outputs, global focus-timestamp ordering after cross-output moves, and fullscreen state for floating-origin command checks.
src/layout/mod.rs, src/layout/workspace.rs, src/layout/tile.rs — expose focused-tree layout mutation and direct container focus for sway runtime commands, including sway's parent targeting, workspace-level layer history, one-level singleton flatten, focus rank and parent preservation across floating transitions, sway's clamped and centered default size and fullscreen clearing for tiled windows entering the scratchpad, and fullscreen state for floating-origin command checks.
src/layout/mod.rs, src/layout/workspace.rs, src/layout/tile.rs, src/window/mapped.rs, src/swayward.rs — expose focused-tree layout mutation and direct container focus for sway runtime commands, including sway's parent targeting, workspace-level layer history, one-level singleton flatten, focus rank and parent preservation across floating transitions, immediate global focus-timestamp ordering across outputs and tiled/floating layers, and fullscreen state for floating-origin command checks.
src/layout/mod.rs, src/layout/workspace.rs, src/layout/tiling_tree/mod.rs, src/layout/tiling_tree/geometry.rs — model workspace and global fullscreen on nested tree containers so focus, movement, rendering, and IPC follow sway's fullscreen ancestry rules.
src/layout/mod.rs, src/layout/tiling_tree/{focus.rs,fullscreen.rs} — keep redundant unfullscreen requests from clearing another node's fullscreen state, stop directional focus at a fullscreen leaf, and select a criteria-targeted global-fullscreen window's workspace, matching sway's fullscreen focus barriers.
src/layout/tiling_tree/{mod.rs,invariants.rs} — route map-time fullscreen state through the single-fullscreen transition and assert its uniqueness, matching sway's replacement of the workspace fullscreen container.
src/layout/tiling_tree/{mod.rs,movement.rs,focus.rs} — apply split and layout commands to an empty workspace root, expose workspace-root focus and active tile state, retain intentional nested and tabbed/stacked singleton wrappers, assign new siblings an equal share, use directional focus and resize by parent layout axis, defer the innermost focus-wrap candidate while searching outer ancestors, insert directional moves by the destination branch axis and inactive child, preserve a moved child's ancestor-relative position when reaping its former parent, preserve MRU focus through removal and reinsertion, preserve source wrappers while directional moves attach and then squash the completed tree, and restore the recorded split axis for `layout default`, matching sway's tree behavior.
swayward-config/src/layout.rs, src/layout/tiling_tree/mod.rs, contrib/sway-to-kdl — support sway's horizontal, vertical, and output-shape-derived auto default orientations when each workspace tree is created.
src/layout/mod.rs, src/window/mapped.rs, src/layout/titlebar.rs — expose window titles to the tree renderer for sway-style server-side titlebars and round each rendered titlebar from its absolute physical endpoints so tab strips stay aligned with their window allocation.
src/ipc/tree.rs, src/layout/floating.rs, src/layout/mod.rs, src/layout/tile.rs, src/window/mapped.rs — retain mapped xdg-shell natural geometry, report it through GET_TREE, restore and constrain it when a window first floats, and expose a pending one-shot resize through IPC, matching sway's natural floating resize.
src/window/mapped.rs, src/swayward.rs, src/handlers/{compositor.rs,xdg_shell.rs}, swayward-config/src/lib.rs, contrib/sway-to-kdl — timestamp urgency, clear it on focus immediately or after sway's cross-workspace timeout, cancel pending timers when windows unmap, and translate `force_display_urgency_hint` without discarding behavior.
src/handlers/xdg_shell.rs — restrict client-requested interactive move and resize to floating windows, and reject fullscreen moves, matching sway's xdg-shell guards.
src/input/mod.rs, src/layout/mod.rs, src/layout/tests.rs — reject compositor-initiated fullscreen drags and focus the destination output when the first drag motion crosses outputs, matching sway's move-seatop entry guards and cross-output drag focus.
swayward-config/src/lib.rs, swayward-config/src/misc.rs, src/handlers/compositor.rs — add sway's `popup_during_fullscreen` policy and apply it to native xdg-toplevel parent relationships at map time; `leave_fullscreen` exits through the layout fullscreen API.
src/swayward.rs, src/layout/mod.rs, src/layout/workspace.rs, src/layout/tiling_tree/mod.rs — resolve pointer hits on tabbed and stacked titlebars to the represented branch's focused descendant before applying focus-follows-mouse.
src/layout/mod.rs, src/layout/monitor.rs, src/layout/workspace.rs — give workspaces stable global sway names/numbers, sparse creation, switching, movement, ordered output assignment with fallback, rename sorting, map-time assignment creation, and sway-compatible inactive-workspace cleanup while retaining monitor animations.
src/layout/mod.rs — resolve relative focus and move-to-workspace targets with sway's global numbered and wrapping per-output workspace ordering.
swayward-config/src/layout.rs, src/layout/{mod.rs,monitor.rs,workspace.rs,tiling_tree/geometry.rs}, swayward-ipc/src/command.rs, src/command/mod.rs, contrib/sway-to-kdl — represent sway outer gaps as per-side workspace-box insets shared by tiled and floating placement, including runtime and per-workspace overrides, negative outer-plus-inner sums, proportional minimum-size clamping, and workspace-owned gap values that survive config reload; do not map them to tiling-only struts.
src/layout/mod.rs — let the i3 conformance restart approximation replace empty transient startup workspace identities while retaining configured persistent workspaces.
src/layout/tiling_tree/geometry.rs — suppress inner gaps for split descendants anywhere below stacked or tabbed containers, and clamp horizontal and vertical gaps to sway's minimum child extents.
src/layout/{tile.rs,floating.rs,tiling_tree/{geometry.rs,rendering.rs}} — square tiled window top corners where a visible tabbed or stacked titlebar strip attaches while preserving configured bottom corners and restoring all corners without the strip.
swayward-config/src/layout.rs, src/layout/{tile.rs,floating.rs,focus_ring.rs,tiling_tree/{mod.rs,geometry.rs,introspection.rs,rendering.rs}}, src/ipc/tree.rs, contrib/sway-to-kdl — represent sway's independent edge and smart border modes, suppress only the selected tiled-window edges in rendering and client geometry, preserve floating borders, and expose matching window rectangles through GET_TREE.
src/layout/mod.rs, src/layout/monitor.rs — retain the previous sway workspace name alongside its live ID so `workspace back_and_forth` can recreate a workspace removed by cleanup, matching sway's seat state; assign each new or newly focused output sway's globally next-free workspace identity; reparent workspaces atomically by stable ID, creating a globally next-free source replacement before detaching the last sway-visible workspace, preserving the trailing empty workspace when attaching content, and sorting the destination before cleanup; prepare and sort content-move destinations before detaching content, carry stable workspace IDs across window, column, subtree, sticky, scratchpad, close, and interactive-move mutations, defer source cleanup until destination attachment, represent prospective drag destinations as monitor-owned geometry and insertion-position previews until drop materializes a real workspace, and route post-transition destruction through one sway-compatible empty-and-inactive check without un-naming attached workspaces.
src/layout/mod.rs, src/layout/monitor.rs — permit only the source workspace of an in-progress interactive move to be transiently empty and unaddressable until the detached window is attached, while retaining sway's destruction invariant outside that transaction.
src/input/{mod.rs,spatial_movement_grab.rs,touch_overview_grab.rs}, src/layout/{mod.rs,monitor.rs,tests.rs}, swayward-config/src/layout.rs — retire niri's built-in vertical workspace-switch swipe and `empty-workspace-above-first`; keep command-driven workspace-switch animation, the additive four-finger overview, client pinch and hold forwarding, and drag-and-drop edge scrolling.
src/layout/mod.rs — when an output disappears, destroy its empty workspaces and evacuate sticky-only contents into the fallback output's active workspace while preserving floating placement across working areas.
src/layout/workspace.rs — keep configured-workspace metadata independent from later sway identity changes; runtime destruction does not consult that metadata, matching sway's three destruction guards.
src/ipc/server.rs — include the Wayland socket name in IPC socket paths so parallel headless tests do not collide, accept sway `binding` and `input` subscriptions with their complete event schemas, and serve live sway-shaped GET_INPUTS and GET_SEATS replies.
src/input/mod.rs, src/input/backend_ext.rs, src/ipc/server.rs, src/swayward.rs — retain backend-neutral input descriptors and the seat name for sway GET_INPUTS, GET_SEATS, and input-event serialization; expose sway's pointer scroll factor plus live USB IDs and supported libinput properties for physical devices.
src/input/mod.rs, src/layout/{mod.rs,workspace.rs,tiling_tree/focus.rs}, src/ipc/server.rs — route wheel-selected tab descendants through global window activation so cross-output focus follows sway, and keep internal active-window bookkeeping out of sway workspace event subscriptions.
src/tests/mod.rs — run vendored i3 Perl assertions against the existing headless compositor and real sway IPC socket.
contrib/sway-to-kdl, swayward-config/src/input.rs, src/ui/hotkey_overlay.rs — translate sway workspace output preferences to named-workspace initial output configuration, expand variables with sway's longest-name-first single-pass semantics, map global inner gaps with sway's optional `px` suffix and negative clamp while refusing inexact outer-gap-to-strut conversion, map `workspace_auto_back_and_forth`, `popup_during_fullscreen`, exact `focus_follows_mouse yes|no`, keyboard `xkb_numlock`, `floating_modifier` normal and none, edge-border modes, and the independent smart-border mode to typed settings, and refuse unrepresentable inverse floating buttons, `focus_follows_mouse always`, and `hide_edge_borders --i3`.
swayward-ipc/src/message.rs — omit absent optional command-result fields to match sway's RUN_COMMAND reply schema.
.github/FUNDING.yml — remove inherited niri sponsorship attribution because swayward is not soliciting sponsorship.
.github/ISSUE_TEMPLATE/bug_report.md, .github/ISSUE_TEMPLATE/config.yml — point issue reporting and support at swayward rather than niri.
.github/workflows/ci.yml — retain only checks that protect supported swayward builds and stop publishing inherited niri documentation.
.github/workflows/ci.yml — install the Perl modules required by the vendored i3 conformance runner.
.github/workflows/release.yml — remove niri's release process until swayward has release artifacts and a versioning policy.
src/layout/mod.rs — export `tiling_tree` and apply cargo-fmt import reordering so the new i3 tree module is compiled and tested.
src/protocols/ext_workspace.rs, src/layout/workspace.rs, src/ipc/tree.rs — expose the same sway-visible workspace set and display-name derivation through GET_WORKSPACES, GET_TREE, and ext-workspace; preserve leave-before-enter ordering on assignment, and ignore assignments to removed output groups.
src/protocols/raw.rs, src/tests/client.rs, src/tests/{foreign_toplevel,layer_shell,xdg_shell}.rs — extend the real Wayland client harness to cover stale foreign-toplevel, layer-output, popup-parent, and inert Mutter X11 interop handles.
src/layout/floating.rs, src/layout/mod.rs, src/layout/workspace.rs — route sway directional resize commands to a specific floating or tiled edge and wrap directional focus among floating window centers while retaining niri's configure-driven floating resize model.
src/layout/floating.rs — keep floating windows, including their rendered borders after runtime border changes, within every exclusive layer-shell boundary while retaining the inherited off-screen allowance at physical output edges.
src/layout/workspace.rs — replace the scrolling tiling field and render element with TilingTree while preserving FloatingSpace.
src/layout/mod.rs — include hidden scratchpad windows in mutable global window traversal so criteria-targeted state changes reach every window exposed by immutable traversal.
src/layout/monitor.rs, src/layout/mod.rs — transfer focused tree tiles rather than concrete scrolling columns between workspaces and outputs.
src/layout/tiling_tree/resize.rs — walk past tabbed and stacked ancestors when resolving width and height adjustment parents, matching sway's strict horizontal/vertical resize-parent search.
src/layout/workspace.rs, src/layout/tiling_tree/resize.rs — propagate failed tiled edge resizes to sway command replies instead of reporting a successful no-op.
src/layout/tiling_tree/focus.rs — keep the focused descendant as the active keyboard window while a split container itself is focused, matching sway's focus-inactive view behavior.
src/layout/tiling_tree/{invariants.rs,mod.rs,rendering.rs} — validate and clear tab indicator node caches with the rest of the tree's NodeId side state, and constrain popup targets to their nested leaf allocation instead of an inherited full-workspace column.
src/layout/mod.rs, src/swayward.rs — resolve directional output moves from the moved window's geometry and wrap to the farthest opposite output, matching wlroots geometry semantics.
src/ipc/server.rs, src/ipc/tree.rs — serve sway GET_TREE, GET_WORKSPACES, and GET_OUTPUTS from live compositor state, including focused empty workspaces, workspace focus membership, sway-compatible workspace number parsing, output-offset floating rectangles, parent-relative floating percentages, and back-to-front stacking order for floating children, instead of niri IPC requests; close events clear the destroyed foreign-toplevel handle to match sway's pre-destruction payload.
src/ipc/server.rs, src/ipc/tree.rs — serve sway GET_TREE, GET_WORKSPACES, and GET_OUTPUTS from live compositor state, including focused empty workspaces, workspace focus membership, sway-compatible workspace number parsing, exact nested-container rectangles, parent-relative decoration rectangles, border-derived window rectangles, parent-relative floating percentages, and back-to-front stacking order for floating children, instead of niri IPC requests; close events clear the destroyed foreign-toplevel handle to match sway's pre-destruction payload.
tests/i3/lib/i3test.pm, tests/i3/lib/i3test/XTEST.pm, tests/i3/bin/setxkbmap, src/tests/i3_conformance.rs — derive the i3 `focused_output` helper from the unmodified sway GET_TREE root focus array, expose native Wayland event-container identity through the same test-side `window` alias used for tree queries and translate its X11 `id` criteria to `con_id`, skip 513's exact two i3-only output-content assertions without fabricating that node, assign sway's default numeric identities for 543's fake outputs and skip its i3-only output-list cycles while preserving direct-move assertions, route 238's xdotool key calls through a test-local executable backed by real headless keyboard events, switch the headless keyboard's real Smithay XKB layout for group-binding coverage while acknowledging the test's external setxkbmap setup, provide upstream's event-based `listen_for_binding` helper for real injected-key diagnostics, translate 531's exact pre-map fullscreen callback plus output-sized rectangle into native `xdg_toplevel.set_fullscreen(output)`, preserve an adapter window's requested pre-map rectangle and forward its fullscreen method through native xdg-toplevel requests, express transient parents with `xdg_toplevel.set_parent`, preserve shutdown IPC subscriptions and route the exit lifecycle through the in-process stop request, find sway's nested synthetic scratchpad workspace, map the floating-size conformance cases initially floating and forward every requested client size while retaining absolute position only as test metadata, translate 194's initial rectangle to its portable xdg-shell content-size part while skipping i3-only outer-size comparisons, skip 133's ICCCM-only aspect constraints without approximating them as independent bounds, keep `launch_with_config('-default')` on the harness's minimal default instead of weakening validation for i3's obsolete default binds, reload the last successfully translated test config through the compositor's in-process reload path, and let unrelated conformance assertions run past the translator's known unsupported-bar warning without weakening production migration diagnostics or changing sway's schema.
src/layout/workspace.rs — expose a read-only TilingTree snapshot for sway GET_TREE serialization.
src/handlers/compositor.rs, src/swayward.rs — apply for_window commands when a toplevel maps, including translated mark, kill, and scratchpad actions without retaining a target after an earlier action removes it; route marks through the global uniqueness machinery; and remove marks when a window unmaps.
src/layout/mod.rs, src/handlers/mod.rs, src/protocols/foreign_toplevel.rs — store sway scratchpad windows and map foreign-toplevel minimize requests to hide/show them; activating a hidden minimized toplevel reveals it through the same scratchpad operation as sway's foreign-toplevel handler.
src/command.rs — apply criteria-targeted scratchpad move, show, and focus commands to the matched window, including revealing hidden scratchpad targets and clearing obstructing fullscreen state, matching sway's overridden-node command context.
src/layout/mod.rs — disable the target workspace and global fullscreen states through `Workspace::set_fullscreen` before showing a scratchpad window, matching sway's scratchpad transition order.
src/command.rs, src/layout/mod.rs — move criteria-matched windows to workspaces without changing focus, move the workspace containing a criteria-matched window or container to an output resolved from that workspace, and ignore hidden scratchpad matches that have no workspace, matching sway's overridden-node command context.
src/command/mod.rs — keep directional moves inside a workspace-fullscreen ancestor instead of falling through to cross-output movement, matching sway's fullscreen move barrier.
src/command/mod.rs — keep native xdg-shell views' X11 `id` criterion unset instead of aliasing their sway container ID.
src/command/mod.rs — do not retarget `floating` from a structurally focused split to its keyboard-focus descendant; floating split containers remain unsupported.
src/command.rs, src/ipc/tree.rs, src/layout/mod.rs, src/layout/monitor.rs, src/layout/workspace.rs, src/layout/tiling_tree/mod.rs — transfer focused container subtrees and workspace-root floating children between workspaces without flattening their structure, losing internal focus history, or dropping floating/fullscreen state; criteria-targeted directional moves cross output boundaries while retaining focus.
swayward-ipc/src/command.rs, src/command.rs, src/layout/{mod.rs,workspace.rs,tiling_tree/resize.rs} — parse and execute sway's full `resize set` grammar, preserve nonpositive dimensions as unchanged, target the nearest matching-axis tiled ancestor for pixel and percentage sizes, reject floating and resize changes to hidden scratchpad windows like sway, guard typed by-id actions from reaching workspace-only mutation paths, and execute sway floating position commands in workspace or global coordinates.
swayward-ipc/src/command.rs, src/command.rs, src/layout/{mod.rs,workspace.rs,tile.rs,tiling_tree/{mod.rs,geometry.rs}}, src/ipc/tree.rs — parse and apply per-window sway border styles and widths, derive untouched tiled and floating GET_TREE widths from the rendered border configuration, and expose the effective state through GET_TREE.
src/layout/mod.rs, src/layout/workspace.rs — fall back to normal placement for children of hidden scratchpad windows and center dialogs from stable parent geometry.
swayward-ipc/src/command.rs, src/command.rs, src/layout/mod.rs, src/layout/monitor.rs, src/layout/tile.rs, src/layout/workspace.rs, src/ipc/tree.rs — implement sway sticky commands and move sticky floating windows with workspace focus on their output.
swayward-ipc/src/command.rs, src/command.rs — parse and execute sway's container and workspace move-to-output forms by output name or direction.
swayward-ipc/src/command.rs, src/command.rs, src/command/layout.rs, src/layout/mod.rs, src/layout/workspace.rs, src/layout/tiling_tree/{mod.rs,tree_layout.rs} — move windows and container subtrees to marked destinations using sway's sibling-versus-child insertion rules while preserving subtree focus and marks; enforce global mark uniqueness across window and split-container storage; remap marks when a layout command flattens a singleton container; swap tiling nodes selected by `con_id` or mark within or across workspaces while preserving focus order, geometry shares, marks, and fullscreen state at the occupied positions.
swayward-ipc/src/command.rs, src/command.rs — parse and execute sway's focus-output command, including multi-word names and directional output lookup; return sway's exact empty-workspace failures for focus-layer, resize, and scratchpad commands instead of silent success.
swayward-ipc/src/command.rs — map sway's zero-argument `splith`, `splitv`, and `splitt` aliases to the existing split commands, parse `focus next|prev sibling` without conflating its no-descent semantics with bare next/prev, parse runtime `exit`, exact manual `urgent` state changes, and top-level per-view shortcut-inhibitor policy, expose headless `create_output`, and keep unsupported urgency-request policy, mutable opacity, user idle-inhibit policy, tearing, and per-view render deadlines fail-loud.
src/window/mapped.rs, src/handlers/mod.rs, src/command/mod.rs — store sway's three-state shortcut-inhibitor policy separately from the protocol object's active lifecycle; disable both rejects future requests and deactivates the current inhibitor, while enable changes future policy only.
src/backend/headless.rs — expose the existing headless output-add path to sway's runtime `create_output` command, using sway's 1920×1080 size and monotonically increasing names.
(workspace-wide) — run `cargo fmt --all` after the rename changed identifier sort order; no behavioural change.

## Deliberate behavioural deviations from sway

src/layout/workspace.rs — retire niri's horizontal viewport offset and its gesture state; the i3 tree always occupies the workspace view.
docs/wiki/, docs/mkdocs.yaml — retire scroll-layout documentation and rebrand retained niri subsystem guides for swayward.
src/layout/mod.rs, src/layout/workspace.rs, src/layout/tiling_tree/mod.rs, src/layout/tiling_tree/resize.rs — expose stable focused-node identity and targeted container layout mutation for sway command contexts, implement sibling-only next/prev focus without descending into the selected container, wrap workspace-level insertions in sway's configured stacking or tabbed default layout, and make `resize set` use outer allocations from the geometry map to resolve sizes against the nearest matching-axis ancestor while distributing its delta across all siblings like sway.
src/handlers/layer_shell.rs — arrange layer surfaces in sway's overlay, top, bottom, background order before Smithay's exclusive and non-exclusive passes, so mixed-layer exclusive zones produce sway-compatible usable areas.
src/ipc/server.rs, src/swayward.rs, swayward-ipc/src/legacy.rs — emit sway-shaped workspace, window, binding-mode, tick, output, and shutdown subscription payloads for waybar-compatible event streams, including the initial `first:true` tick after subscription, `output::unspecified` after output configuration changes, `shutdown::exit` before event-loop stop, and ordered workspace init, focus, rename, empty, reload, close, move, floating, fullscreen, title, mark, new, and urgency events; successful config reloads emit sway's captured workspace reload payload even when workspace state is unchanged.
swayward-config/src/binds.rs, src/input/backend_ext.rs, src/input/mod.rs — add sway command-string binds stored without eager command validation, matching sway's parse-now/execute-later binding lifecycle, including exclusive press/release edges, owned release bindings cancelled when another press ends their chord, mode-local binding lookup, input-device and XKB-group wildcards with sway's precedence, translated-keysym precedence, exact Num/Caps matches with lock-insensitive fallback, and XKB-domain bindcodes matched against the modifier snapshot from before the current key transition, while retaining inherited typed actions for unsupported compositor features. Input-device matching derives sway's `vendor:product:name` identifier from Smithay's USB IDs and device name, including sway's whitespace normalization; non-libinput backends use sway's zero IDs.
swayward-config/src/binds.rs, src/input/mod.rs, src/ui/hotkey_overlay.rs — accept numeric bindcode triggers so translated sway bindcode directives remain functional.
swayward-ipc/src/command.rs, swayward-config/src/lib.rs, swayward-config/src/binds.rs, src/swayward.rs, src/input/mod.rs, src/command.rs, src/ipc/server.rs — support named binding modes, mode commands and events, GET_BINDING_MODES, and sway's full `layout toggle` grammar.
swayward-ipc/src/command.rs, src/command/, src/window/mapped.rs, src/layout/tiling_tree/, src/ipc/tree.rs, src/handlers/{mod.rs,xdg_shell.rs}, src/swayward.rs — store per-window sway `title_format`, expand all eight sway placeholders in one pass for GET_TREE and titlebar rendering, retain Wayland security-context metadata, and refresh the formatted title when client metadata changes.
resources/default-config.kdl — replace scroll-layout and column-oriented default binds with sway's tree-oriented defaults; inherited typed actions remain available to existing configs.
contrib/dev-run.sh, contrib/test-dev-run.sh, docs/BUILDING.md — isolate nested development runs from the operator's user config by copying the repository default into the private run directory unless `--config` is explicit.
resources/default-config.kdl — mention every user-facing config node accepted by the parser, mostly in disabled examples, so the shipped config exposes available features without changing defaults.
src/ui/mru.rs — retain inherited typed focus actions as MRU navigation aliases for existing user configs; shipped sway-command binds do not generate these aliases.

src/layout/mod.rs, src/layout/monitor.rs, src/ui/mru.rs, swayward-config/src/animations.rs — keep niri's workspace overview and its MRU window switcher as additive features sway does not have, including their `OverviewOpenCloseAnim` and `RecentWindowsCloseAnim` settings; retire the dead `HorizontalViewMovementAnim` setting with the scrolling engine. Both retained features are opt-in: the overview needs `ToggleOverview` or a four-finger swipe, the switcher needs an `MruAdvance` bind. Neither adds sway-visible state, appears in `GET_TREE` or `GET_WORKSPACES`, nor touches workspace identity or ordering, so a sway client cannot observe either one. The foundation spec originally retired `OverviewOpenCloseAnim` with the scrolling engine; that was wrong, because an overview is layout-agnostic rather than scroll-specific, and the spec is corrected.
src/input/mod.rs — make every overview arrow key switch workspaces directly because niri's window-first and column focus actions are mostly invisible in swayward's tree layout.
src/layout/mod.rs, src/layout/monitor.rs, src/layout/workspace.rs, src/layout/tiling_tree/{mod.rs,introspection.rs} — hit-test the nested container under a tiling drag pointer, insert beside it according to sway's nearest-edge rule, and exchange containers when the drag ends in its centre.

src/input/mod.rs, src/layout/monitor.rs — retire niri's vertical three-finger workspace-switch swipe. Sway has no built-in workspace gesture (`sway/input/seatop_default.c` contains no swipe handling near workspace transitions), and that gesture is the sole justification for niri's trailing placeholder workspace, which blocks sway's workspace lifecycle. Gesture *forwarding* is unaffected: pinch and hold events still reach clients as they do under sway, and the four-finger overview swipe is kept. `bindgesture` remains refused by the translator, unchanged.
src/layout/mod.rs, src/layout/monitor.rs, src/layout/workspace.rs — stop appending and preserving niri's client-visible trailing placeholder workspace; sway creates workspaces on demand and destroys empty inactive ones. Keep drag creation as transient preview state that materializes a real workspace only on drop.
CONTRIBUTING.md, resources/default-config.kdl, resources/swayward.desktop, resources/swayward.service, swayward-ipc/README.md, swayward-visual-tests/README.md, docs/wiki/Configuration:-Animations.md, docs/wiki/Configuration:-Debug-Options.md, docs/wiki/Configuration:-Input.md, docs/wiki/Configuration:-Outputs.md, docs/wiki/Configuration:-Recent-Windows.md, docs/wiki/Configuration:-Window-Rules.md — remove inherited branding and descriptions of the retired scrollable layout from user-facing text.
contrib/sway-to-kdl, swayward-config/src/layout.rs, src/command/focus.rs, src/layout/tiling_tree/focus.rs — translate all four sway focus-wrapping modes, preserve forced inner-container wrapping, suppress local wrap candidates for `no`, and block adjacent-output traversal for `workspace` unless the workspace node is focused; unknown modes remain fail-loud instead of inheriting sway's accidental `no` fallback.
contrib/sway-to-kdl — report `bindgesture`, `unbindgesture`, `bindswitch`, and `unbindswitch` with specific unsupported-semantics diagnostics instead of mapping them to narrower built-in gestures or spawn-only switch events; retain `client.background` and `client.placeholder` as source-located comments explaining that sway itself accepts and ignores them.
contrib/sway-to-kdl, swayward-config/src/window_rule.rs, src/window/mod.rs, src/layout/mod.rs, src/layout/monitor.rs, src/handlers/xdg_shell.rs, src/layout/tile.rs, src/handlers/compositor.rs — translate sway's default border directives into initial tiled and floating border rules without changing swayward's shipped visual defaults; also translate combined app-id/title/current tiling-state criteria and distinct workspace-name, workspace-number, and output assignment targets, floating, layout, focus, `nop`, `no_focus`, `focus_on_window_activation` urgent/focus/none, mouse-warping, mouse-region bind options before or after the key, and workspace-placement rules without altering regex escapes, while failing loudly on X11-only criteria, i3-only tiling/floating provenance criteria, visibility-dependent activation policy, and unsupported rule actions.
src/ipc/tree.rs — expose each mapped window's real urgency in GET_TREE, matching sway's common node schema and allowing xdg-activation urgency to be observed over IPC.
Cargo.toml, resources/swayward-portals.conf — keep niri's GNOME-first portal policy as an intentional product deviation from sway: the Mutter integration provides the window picker and dynamic cast target, while xdg-desktop-portal-wlr remains an optional fallback; recommend the GNOME and GTK backends, gnome-keyring, and the Nautilus 47+ FileChooser implementation in package metadata.
tests/i3/lib/i3test.pm, src/tests/i3_conformance.rs, tests/i3/coverage.toml, tests/i3/README.md — remove adapter branches that converted named, numbered, and subtest assertions into TAP skips before comparing values; unsupported operations now fail or remain unreached, and coverage records fresh real TAP outcomes.
src/tests/i3_conformance.rs, tests/i3/coverage.toml, tests/i3/README.md — stop treating `176-workspace-baf.t` as fully green when its restart command is rejected; classify the post-restart assertion as unproven because sway has no runtime restart and the in-process fixture cannot preserve clients across compositor replacement.
tests/i3/lib/i3test.pm, tests/i3/coverage.toml, tests/i3/README.md — stop rewriting `100-fullscreen.t` GET_TREE replies to fabricate i3's output content node; measure and classify the unchanged test against sway's direct-workspace output tree.
src/tests/i3_conformance.rs, tests/i3/coverage.toml — configure the `257-keypress-group1-fallback.t` fixture with the `us,ru` keymap requested by its external `setxkbmap` setup, so its group-2 binding checks run against a real second XKB group rather than the adapter's no-op executable alone.
tests/i3/coverage.toml, tests/i3/README.md — stop crediting vacuous setup checks in `102-dock.t`, `222-regress-dock-resize.t`, and `268-ipc-config.t`: classify them with citations because the adapter never establishes the X11 dock or separate-compositor premises they are meant to guard.
tests/i3/coverage.toml, tests/i3/README.md — stop crediting `141-resize.t` assertion 83's comparison of two absent floating-wrapper child rectangles; sway omits i3's wrapper and exposes the floating leaf directly.
tests/i3/lib/i3test.pm, tests/i3/coverage.toml, tests/i3/README.md — reject unsupported X11 `window_type` arguments instead of silently mapping ordinary xdg-toplevels; remeasure the dock regressions whose green liveness checks depended on that silent substitution.
contrib/coverage-report, contrib/tap-count, tests/i3/coverage.toml, tests/i3/README.md — require every coverage row to record its captured TAP plan, distinguish no-plan aborts from planned runs, and record harness `1..0 # SKIP` outcomes as cited file-level skips instead of pretending their static source assertions ran.
src/layout/mod.rs — report fullscreen mode only for the fullscreen node and its descendants, matching sway's per-container state instead of attributing one workspace member's fullscreen mode to every window on that workspace.
src/layout/tiling_tree/tests.rs — run 16 generated tiling-tree invariant cases in the default fast test gate while retaining the environment-controlled full search for slow CI; checked-in proptest regression seeds continue to replay before generated cases.
